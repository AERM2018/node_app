# notas:

Primera aplicacion de Node.js
```
Options:
      --help     Show help                                             [boolean]
      --version  Show version number                                   [boolean]
  -b, --base     Es la base de la tabla de multiplicar       [number] [required]
  -l, --listar   Opcion que enlista o no la tabla     [boolean] [default: false]
  -h, --hasta    Establecer un limite para la tabla de multiplicar
                                                          [number] [default: 10]
```